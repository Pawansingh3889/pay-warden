.env
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
pay_warden_audit.sqlite3
pay_warden_demo.sqlite3

# Generated by scripts/simulate.py
policies/simulated.yaml
pay_warden_simulated.sqlite3

# Local demo policy for the steward integration. Written and then mutated at
# runtime (steward adds spenders to it), so it is state, not configuration.
# steward gitignores its own copy for the same reason; policies/default.yaml is
# the tracked example.
demo-household.yaml
